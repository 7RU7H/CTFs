/etc/fail2ban/action.d/mail-whois.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/iptables-ipset-proto4.conf:#   module-assistant auto-install xtables-addons
/etc/fail2ban/action.d/sendmail-buffered.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/abuseipdb.conf:#     password incorrectly.
/etc/fail2ban/action.d/sendmail-whois-ipmatches.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/mail.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/sendmail-whois-ipjailmatches.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/sendmail-geoip-lines.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/xarf-login-attack.conf:#     password incorrectly.
/etc/fail2ban/action.d/xarf-login-attack.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/xarf-login-attack.conf:message = Dear Sir/Madam,\n\nWe have detected abuse from the IP address $IP, which according to abusix.com is on your network. We would appreciate if you would investigate and take action as appropriate.\n\nLog lines are given below, but please ask if you require any further information.\n\n(If you are not the correct person to contact about this please accept our apologies - your e-mail address was extracted from the whois record by an automated process.)\n\n This mail was generated by Fail2Ban in a X-ARF format! You can find more information about x-arf at http://www.x-arf.org/specification.html.\n\nThe recipient address of this report was provided by the Abuse Contact DB by abusix.com. abusix.com does not maintain the content of the database. All information which we pass out, derives from the RIR databases and is processed for ease of use. If you want to change or report non working abuse contacts please contact the appropriate RIR. If you have any further question, contact abusix.com directly via email (info@abusix.com). Information about the Abuse Contact Database can be found here: https://abusix.com/global-reporting/abuse-contact-db\nabusix.com is neither responsible nor liable for the content or accuracy of this message.\n
/etc/fail2ban/action.d/xarf-login-attack.conf:# Notes.:  Your system mail command. It is passed the recipient
/etc/fail2ban/action.d/xarf-login-attack.conf:#          arguments to be passed to Sendmail:
/etc/fail2ban/action.d/sendmail-whois.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/route.conf:#   - It's per host, ideal as action against ssh password bruteforcing to block further attack attempts.
/etc/fail2ban/action.d/route.conf:#   - Blocking is per IP and NOT per service, but ideal as action against ssh password bruteforcing hosts
/etc/fail2ban/action.d/mail-buffered.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/firewallcmd-common.conf:#          kpasswd ldap ldaps libvirt libvirt-tls mdns mosh mountd ms-wbt mysql nfs ntp openvpn pmcd pmproxy pmwebapi pmwebapis pop3s 
/etc/fail2ban/action.d/sendmail-whois-lines.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/badips.py:class BadIPsAction(ActionBase): # pragma: no cover - may be unavailable
/etc/fail2ban/action.d/badips.py:		Name assigned to the action.
/etc/fail2ban/action.d/badips.py:		associated key.
/etc/fail2ban/action.d/badips.py:			associated key.
/etc/fail2ban/action.d/complain.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/complain.conf:message = Dear Sir/Madam,\n\nWe have detected abuse from the IP address $IP, which according to a abusix.com is on your network. We would appreciate if you would investigate and take action as appropriate.\n\nLog lines are given below, but please ask if you require any further information.\n\n(If you are not the correct person to contact about this please accept our apologies - your e-mail address was extracted from the whois record by an automated process.)\n\n This mail was generated by Fail2Ban.\nThe recipient address of this report was provided by the Abuse Contact DB by abusix.com. abusix.com does not maintain the content of the database. All information which we pass out, derives from the RIR databases and is processed for ease of use. If you want to change or report non working abuse contacts please contact the appropriate RIR. If you have any further question, contact abusix.com directly via email (info@abusix.com). Information about the Abuse Contact Database can be found here: https://abusix.com/global-reporting/abuse-contact-db\nabusix.com is neither responsible nor liable for the content or accuracy of this message.\n
/etc/fail2ban/action.d/complain.conf:# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
/etc/fail2ban/action.d/complain.conf:#          arguments to be passed to Sendmail:
/etc/fail2ban/action.d/netscaler.conf:# The script assumes using HTTPS with unsecure certificate to access the netscaler, 
/etc/fail2ban/action.d/netscaler.conf:# ns_auth: username:password, suggest base64 encoded for a little added security (echo -n "username:password" | base64)
/etc/fail2ban/action.d/sendmail-whois-matches.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/sendmail.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/nsupdate.conf:# The ban and unban commands assume nsupdate will authenticate to the BIND
/etc/fail2ban/action.d/helpers-common.conf:_bypass_if_restored = if [ '<restored>' = '1' ]; then exit 0; fi;
/etc/fail2ban/action.d/smtp.py:class SMTPAction(ActionBase):
/etc/fail2ban/action.d/smtp.py:		self, jail, name, host="localhost", user=None, password=None,
/etc/fail2ban/action.d/smtp.py:			Named assigned to the action.
/etc/fail2ban/action.d/smtp.py:		password : str, optional
/etc/fail2ban/action.d/smtp.py:			Password used for authentication with SMTP server.
/etc/fail2ban/action.d/smtp.py:		self.password =password
/etc/fail2ban/action.d/smtp.py:		# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/smtp.py:			if self.user and self.password:
/etc/fail2ban/action.d/smtp.py:				smtp.login(self.user, self.password)
/etc/fail2ban/action.d/smtp.py:				pass # Not connected
/etc/fail2ban/action.d/dshield.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/dshield.conf:#          is the first IP assigned to eth0
/etc/fail2ban/action.d/dshield.conf:# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
/etc/fail2ban/action.d/dshield.conf:#          passed to Sendmail):
/etc/fail2ban/action.d/mynetwatchman.conf:# <mnwpass> (your mNW password).
/etc/fail2ban/action.d/mynetwatchman.conf:# mnwpass = SECRET
/etc/fail2ban/action.d/mynetwatchman.conf:            MNWPASS=`perl -e '$s=shift;$s=~s/([\W])/"%%".uc(sprintf("%%2.2x",ord($1)))/eg;print $s' '<mnwpass>'`
/etc/fail2ban/action.d/mynetwatchman.conf:            <getcmd> "<mnwurl>?AT=2&AV=0&AgentEmail=$MNWLOGIN&AgentPassword=$MNWPASS&AttackerIP=<ip>&SrcPort=<srcport>&ProtocolID=$PROTOCOL&DestPort=<port>&AttackCount=<failures>&VictimIP=<myip>&AttackDateTime=$DATETIME" 2>&1 >> <tmpfile>.out && grep -q 'Attack Report Insert Successful' <tmpfile>.out && rm -f <tmpfile>.out
/etc/fail2ban/action.d/mynetwatchman.conf:# Option:  mnwpass
/etc/fail2ban/action.d/mynetwatchman.conf:# Notes.:  The password corresponding to your mNW login e-mail address. MUST be
/etc/fail2ban/action.d/mynetwatchman.conf:mnwpass =
/etc/fail2ban/action.d/mynetwatchman.conf:#          is the first IP assigned to eth0
/etc/fail2ban/action.d/blocklist_de.conf:# This action requires the server 'email address' and the associated apikey.
/etc/fail2ban/action.d/blocklist_de.conf:#     password incorrectly.
/etc/fail2ban/action.d/mail-whois-lines.conf:# bypass ban/unban for restored tickets
/etc/fail2ban/action.d/mail-whois-lines.conf:# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
# bypass ban/unban for restored tickets
#   module-assistant auto-install xtables-addons
# bypass ban/unban for restored tickets
#     password incorrectly.
# bypass ban/unban for restored tickets
# bypass ban/unban for restored tickets
# bypass ban/unban for restored tickets
# bypass ban/unban for restored tickets
#     password incorrectly.
# bypass ban/unban for restored tickets
message = Dear Sir/Madam,\n\nWe have detected abuse from the IP address $IP, which according to abusix.com is on your network. We would appreciate if you would investigate and take action as appropriate.\n\nLog lines are given below, but please ask if you require any further information.\n\n(If you are not the correct person to contact about this please accept our apologies - your e-mail address was extracted from the whois record by an automated process.)\n\n This mail was generated by Fail2Ban in a X-ARF format! You can find more information about x-arf at http://www.x-arf.org/specification.html.\n\nThe recipient address of this report was provided by the Abuse Contact DB by abusix.com. abusix.com does not maintain the content of the database. All information which we pass out, derives from the RIR databases and is processed for ease of use. If you want to change or report non working abuse contacts please contact the appropriate RIR. If you have any further question, contact abusix.com directly via email (info@abusix.com). Information about the Abuse Contact Database can be found here: https://abusix.com/global-reporting/abuse-contact-db\nabusix.com is neither responsible nor liable for the content or accuracy of this message.\n
# Notes.:  Your system mail command. It is passed the recipient
#          arguments to be passed to Sendmail:
# bypass ban/unban for restored tickets
#   - It's per host, ideal as action against ssh password bruteforcing to block further attack attempts.
#   - Blocking is per IP and NOT per service, but ideal as action against ssh password bruteforcing hosts
# bypass ban/unban for restored tickets
#          kpasswd ldap ldaps libvirt libvirt-tls mdns mosh mountd ms-wbt mysql nfs ntp openvpn pmcd pmproxy pmwebapi pmwebapis pop3s 
# bypass ban/unban for restored tickets
class BadIPsAction(ActionBase): # pragma: no cover - may be unavailable
		Name assigned to the action.
		associated key.
			associated key.
# bypass ban/unban for restored tickets
message = Dear Sir/Madam,\n\nWe have detected abuse from the IP address $IP, which according to a abusix.com is on your network. We would appreciate if you would investigate and take action as appropriate.\n\nLog lines are given below, but please ask if you require any further information.\n\n(If you are not the correct person to contact about this please accept our apologies - your e-mail address was extracted from the whois record by an automated process.)\n\n This mail was generated by Fail2Ban.\nThe recipient address of this report was provided by the Abuse Contact DB by abusix.com. abusix.com does not maintain the content of the database. All information which we pass out, derives from the RIR databases and is processed for ease of use. If you want to change or report non working abuse contacts please contact the appropriate RIR. If you have any further question, contact abusix.com directly via email (info@abusix.com). Information about the Abuse Contact Database can be found here: https://abusix.com/global-reporting/abuse-contact-db\nabusix.com is neither responsible nor liable for the content or accuracy of this message.\n
# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
#          arguments to be passed to Sendmail:
# The script assumes using HTTPS with unsecure certificate to access the netscaler, 
# ns_auth: username:password, suggest base64 encoded for a little added security (echo -n "username:password" | base64)
# bypass ban/unban for restored tickets
# bypass ban/unban for restored tickets
# The ban and unban commands assume nsupdate will authenticate to the BIND
_bypass_if_restored = if [ '<restored>' = '1' ]; then exit 0; fi;
class SMTPAction(ActionBase):
		self, jail, name, host="localhost", user=None, password=None,
			Named assigned to the action.
		password : str, optional
			Password used for authentication with SMTP server.
		self.password =password
		# bypass ban/unban for restored tickets
			if self.user and self.password:
				smtp.login(self.user, self.password)
				pass # Not connected
# bypass ban/unban for restored tickets
#          is the first IP assigned to eth0
# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
#          passed to Sendmail):
# <mnwpass> (your mNW password).
# mnwpass = SECRET
            MNWPASS=`perl -e '$s=shift;$s=~s/([\W])/"%%".uc(sprintf("%%2.2x",ord($1)))/eg;print $s' '<mnwpass>'`
            <getcmd> "<mnwurl>?AT=2&AV=0&AgentEmail=$MNWLOGIN&AgentPassword=$MNWPASS&AttackerIP=<ip>&SrcPort=<srcport>&ProtocolID=$PROTOCOL&DestPort=<port>&AttackCount=<failures>&VictimIP=<myip>&AttackDateTime=$DATETIME" 2>&1 >> <tmpfile>.out && grep -q 'Attack Report Insert Successful' <tmpfile>.out && rm -f <tmpfile>.out
# Option:  mnwpass
# Notes.:  The password corresponding to your mNW login e-mail address. MUST be
mnwpass =
#          is the first IP assigned to eth0
# This action requires the server 'email address' and the associated apikey.
#     password incorrectly.
# bypass ban/unban for restored tickets
# Notes.:  Your system mail command. Is passed 2 args: subject and recipient
